# Implementation Plan - UI Tweaks & Security Port (v3.2.1)

## 1. UI Updates
- [ ] **HTML (`templates/index.html`)**:
    - [ ] Rename result header ID to `res-title`.
    - [ ] Add `res-samvatsara` grid item (grouped with Vara).
- [ ] **JavaScript (`static/js/main.js`)**:
    - [ ] Update `renderResult` to populate `res-title` with user input.
    - [ ] Update `renderResult` to populate `res-samvatsara`.

## 2. Security Updates (Port 58921)
- [ ] **Nginx Config (`panchanga.nginx.template`)**:
    - [ ] Change `listen 443 ssl` to `listen 58921 ssl`.
    - [ ] Remove HTTP redirect on port 80? (Or keep it but it won't work for custom port easily without port in URL). Let's Keep 80 -> 443 active? No, USER wanted RANDOM port. So we likely disable standard ports or redirect 80 -> 58921.
    - [ ] **Decision**: Redirect 80 to 58921 for ease of use? Or strictly just 58921. Stricter is better. Remove Port 80 block.

- [ ] **Deployment Script (`deploy.sh`)**:
    - [ ] Add firewall rule for `58921`.
    - [ ] Add SELinux `semanage port` for `58921` (http_port_t).
    - [ ] Remove old firewall rules for 80/443/5080 to reduce surface area? (Optional but good).

## 3. Verification
- [ ] Verify UI title logic.
- [ ] Verify access on `https://<IP>:58921`.
